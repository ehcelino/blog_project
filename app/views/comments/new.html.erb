<%= form_with model: @comment do |c| %>
  <dl>
    <dt>
      <%= c.label 'Título'%>
      <%= c.text_field :title %>
    </dt>
    <dt>
      <%= c.label 'Texto'%>
      <%= c.text_area :body, rows: 15, cols: 50 %>
    </dt>
    <dt>
      <%= c.hidden_field :user_id, value: current_user.id %>
      <%= c.hidden_field :post_id, value: @post.id %>
    </dt>
    <dt>
      <%= c.submit 'Salvar' %>
    </dt>
  </dl>
<% end %>